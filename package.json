{
  "name": "line-deepseek-bot-upgraded",
  "version": "1.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node index.upgraded.js",
    "dev": "nodemon index.upgraded.js",
    "lint": "eslint . --ext .js",
    "test": "echo \"No tests yet\" && exit 0",
    "build": "echo build step (none)"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "axios": "^1.5.0",
    "dotenv": "^16.0.0",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "nodemon": "^2.0.22",
    "eslint": "^8.48.0"
  }
}
